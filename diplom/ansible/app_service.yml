---
apiVersion: v1
kind: Service
metadata:
  name: test-app-svc
  labels:
    app: test-app
spec:
  selector:
    app: test-app
  ports:
    - name: test-app-http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32200
  type: NodePort